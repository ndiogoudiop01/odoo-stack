# ---------------------------------------------------------------------------
#  Dépendances Python présentes dans TOUTES les images de base.
#  Ce qui est spécifique à un client va dans clients/<slug>/requirements.txt.
#
#  Épinglez toujours (==) : une image de base doit être reproductible.
# ---------------------------------------------------------------------------

# Débogage à distance (VSCode / PyCharm) — activé par ODOO_DEBUGPY=1
debugpy==1.8.5

# Driver PostgreSQL en binaire : évite toute recompilation côté client
psycopg2-binary==2.9.9

# Montants en lettres (factures francophones, chèques)
num2words==0.5.13

# Normalisation des numéros de téléphone (+221, +225, +226…)
phonenumbers==8.13.44

# Validation d'identifiants nationaux / TVA
python-stdnum==1.20

# Exports Excel avancés
xlsxwriter==3.2.0

# SFTP (échanges bancaires, EDI)
paramiko==3.4.1
